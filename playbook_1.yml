---
- hosts: localhost
  remote_user: root
  roles:
    - role: ansible.kubernetes-modules
    - role: hello-world

  tasks:
    - name: run ansible command
      file:
       path: /home/edureka/sanjeev/gitfile.txt
       state: touch
    
    - name: ansible k8s module
      k8s:
        state: present
        api_version: v1
        kind: Namespace
