---
- hosts: localhost
  connection: local

  tasks:
    - name: run ansible command
      file:
       path: /home/edureka/sanjeev/gitfile.txt
       state: touch
    - name: run kubernet job
      become: yes
      command: kubectl apply -f pod-01.yml
